apiVersion: apps/v1
kind: Deployment
metadata:
  name: favourite-service
  namespace: stage
  labels:
    app: favourite-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: favourite-service
  template:
    metadata:
      labels:
        app: favourite-service
        version: v1
    spec:
      imagePullSecrets:
        - name: dockerhub-credentials
      containers:
        - name: favourite-service
          image: merako34/favourite-service:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8800
              name: http
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "stage"
            - name: SPRING_APPLICATION_NAME
              value: "FAVOURITE-SERVICE"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "250m"