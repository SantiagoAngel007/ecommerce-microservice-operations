# proxy-client-service.yaml (PROD)
apiVersion: v1
kind: Service
metadata:
  name: proxy-client
  namespace: prod
  labels:
    app: proxy-client
spec:
  type: ClusterIP
  selector:
    app: proxy-client
  ports:
  - name: http
    port: 8200
    targetPort: 8200
    protocol: TCP