apiVersion: v1
kind: Service
metadata:
  name: payment-service-svc
  namespace: dev
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 84
      targetPort: 84
      protocol: TCP

